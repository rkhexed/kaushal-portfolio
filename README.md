# Kaushal Subramani, portfolio

Personal site: work, research (with three live in-browser demos), and a photo roll.

```sh
npm install
npm run dev      # http://localhost:4321
npm run build    # static output in dist/
```

Built with Astro, GSAP and Lenis. Photos are optimized at build time from `src/assets/photos/`.
